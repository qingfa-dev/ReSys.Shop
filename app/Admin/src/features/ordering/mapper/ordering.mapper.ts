import type { OrderListItem, OrderDetail } from '../types/order.types'

export function mapOrderListItem(data: OrderListItem): OrderListItem {
  return data
}

export function mapOrderDetail(data: OrderDetail): OrderDetail {
  return data
}
