"""CLI entry points registered in pyproject.toml ``[project.scripts]``.

The Typer app is defined in ``benchmark.py`` and wrapped in ``main.py``.
The ``benchmark`` console script entry point delegates to ``main:app``.
"""
