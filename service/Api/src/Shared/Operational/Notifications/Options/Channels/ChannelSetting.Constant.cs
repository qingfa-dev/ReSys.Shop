namespace Shared.Operational.Notifications.Options.Channels;

/// <summary>Constants and default values for channel configuration.</summary>
public static class ChannelConstant
{
    /// <summary>Default configuration values for notification channels.</summary>
    public static class Defaults
    {
        public const bool Enabled = true;
    }
}
