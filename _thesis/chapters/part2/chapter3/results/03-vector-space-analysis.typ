=== Latent Space Quality

To further validate the semantic quality of the embeddings, we visualized the high-dimensional vector space using t-SNE dimensionality reduction.

#figure(
  image("/images/charts/results/tsne-clusters.png", width: 90%),
  caption: [t-SNE Visualization of Fashion-CLIP Embeddings. Distinct clusters (e.g., "Dresses" vs. "Shoes") confirm that the model effectively learns semantic category separation.],
)
