# =============================================================================
# ReSys.Shop API Test - Admin Catalog Variants
# =============================================================================
# @baseUrl          = {{baseUrl}}
# @contentType      = {{contentType}}
# @adminToken       = {{adminToken}}
# @nonExistentId    = {{nonExistentId}}
# @existingProductId = {{existingProductId}}
# @existingVariantId = {{existingVariantId}}
# =============================================================================

### ---------------------------------------------------------------------------
### 1. Add variant – success
# POST /api/catalog/products/{productId}/variants
# Expected: 201 Created with location header
# ---------------------------------------------------------------------------
POST {{baseUrl}}/api/catalog/products/{{existingProductId}}/variants
Content-Type: {{contentType}}
Authorization: Bearer {{adminToken}}

{
  "sku": "TSH-001-S",
  "price": 29.99,
  "currency": "USD",
  "isMaster": false
}

### ---------------------------------------------------------------------------
### 2. Add variant – duplicate SKU
# POST /api/catalog/products/{productId}/variants
# Expected: 409 Conflict – SKU already exists
# ---------------------------------------------------------------------------
POST {{baseUrl}}/api/catalog/products/{{existingProductId}}/variants
Content-Type: {{contentType}}
Authorization: Bearer {{adminToken}}

{
  "sku": "TSH-001-S",
  "price": 24.99,
  "currency": "USD",
  "isMaster": false
}

### ---------------------------------------------------------------------------
### 3. Add variant – validation error
# POST /api/catalog/products/{productId}/variants
# Expected: 400 Bad Request – SKU is required
# ---------------------------------------------------------------------------
POST {{baseUrl}}/api/catalog/products/{{existingProductId}}/variants
Content-Type: {{contentType}}
Authorization: Bearer {{adminToken}}

{
  "sku": "",
  "price": -5.00,
  "currency": "USD",
  "isMaster": false
}

### ---------------------------------------------------------------------------
### 4. List variants by product (paged)
# GET /api/catalog/products/{productId}/variants
# Expected: 200 OK – paged list of variants
# ---------------------------------------------------------------------------
GET {{baseUrl}}/api/catalog/products/{{existingProductId}}/variants?page={{page}}&pageSize={{pageSize}}
Content-Type: {{contentType}}
Authorization: Bearer {{adminToken}}

### ---------------------------------------------------------------------------
### 5. Update variant – success
# PUT /api/catalog/products/variants/{id}
# Expected: 200 OK – updated variant
# ---------------------------------------------------------------------------
PUT {{baseUrl}}/api/catalog/products/variants/{{existingVariantId}}
Content-Type: {{contentType}}
Authorization: Bearer {{adminToken}}

{
  "sku": "TSH-001-M",
  "price": 34.99,
  "currency": "USD",
  "isMaster": false
}

### ---------------------------------------------------------------------------
### 6. Delete variant – success
# DELETE /api/catalog/products/variants/{id}
# Expected: 200 OK – variant deleted
# ---------------------------------------------------------------------------
DELETE {{baseUrl}}/api/catalog/products/variants/{{existingVariantId}}
Content-Type: {{contentType}}
Authorization: Bearer {{adminToken}}

### ---------------------------------------------------------------------------
### 7. No authentication – 401
# GET /api/catalog/products/{productId}/variants
# Expected: 401 Unauthorized – missing or invalid token
# ---------------------------------------------------------------------------
GET {{baseUrl}}/api/catalog/products/{{existingProductId}}/variants
Content-Type: {{contentType}}
